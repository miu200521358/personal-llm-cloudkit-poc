# AGENTS.md  
Codex エージェント開発規約  
（personal-llm-cloudkit-poc）

この文書は、Codex エージェントが本リポジトリで開発作業を行う際の  
**行動規範・出力ルール・ファイル操作ルール・Issueドリブンの遵守事項** を定義する。

エージェントは、このドキュメントの内容を常に遵守すること。

---

# 1. 基本方針

1. 本リポジトリは **Issue ドリブン開発** を採用する。  
   エージェントは、ユーザーから明確な指示が無い限り、  
   **「指定された Issue の内容のみ」** を実装し、他の範囲に影響を与えてはならない。

2. エージェントは **推測で実装を追加してはならない**。  
   - Issue のスコープ外の変更は行わない  
   - 必要な前提が不足している場合は質問する

3. 出力は常に **完全なファイル単位** または **差分パッチ形式** のいずれか、  
   Issue 内で指定された形式に従うこと。

4. 既存コードの削除が必要な場合は、  
   **必ず理由を明記し、誤って削除していないことを示す**。

---

# 2. Issue ドリブン開発ルール

1. エージェントは `/plan` で指定された Issue 番号の **Issue 本文のみ** を仕様として扱う。  
   他の Issue や README の内容は参照してはならない。

2. Issue 内で「この Issue のみ実装」「他は触らない」と明記された場合、  
   **その範囲外の変更は禁止**。

3. Issue に「後続 Issue で扱う」と書かれている内容は  
   **現在の Issue では絶対に実装しない**。

4. Issue の「Definition of Done」を満たすよう正確に実装する。

---

# 3. ファイル編集ルール

1. Issue が複数ファイルの変更を要求する場合は、  
   **ファイルごとに区切って完全版を出力**する。

2. 「追記のみ」が求められている場合は、  
   **既存コードを壊さず、指定位置に正確に追記**する。

3. ディレクトリ構造や命名を勝手に変更してはならない。

4. ユーザーが「diffで」と指定した場合は  
   **Unified diff** で出力する。

---

# 4. Terraform / GitHub Actions に関するルール

1. Terraform の変数名・リソース名・outputs 名は  
   **Issue #1（全体仕様）で定めた命名に厳密に従う**。

2. リソースの依存関係（Cloud Run → IAM → GCS など）は  
   Issue の指示順に従い、勝手に結合・分割しない。

3. GitHub Actions ワークフロー生成時は、  
   - Secrets 名  
   - 成功/失敗条件  
   - Step 依存  
   を Issue に従って正確に記述する。

4. 不要なステップ追加は禁止。

---

# 5. 応答フォーマットの厳密性

1. エージェントは Issue が指定したフォーマットでのみ出力する。  
   - YAML 全文  
   - HCL 全文  
   - diff  
   - Markdown 内のエスケープ etc.

2. 説明文が不要な場合は **いかなる説明も出力しない**。

3. Markdown 中のコードブロックは  
   **Issue で指定されたエスケープ方式**に従う。

---

# 6. PR（プルリクエスト）提出時の必須ルール

エージェントが PR を作成する場合、  
**必ず `.github/pull_request_template.md` のテンプレートに従って PR を作成すること。**

## 6-1. PR テンプレート遵守

- PR 作成時は、`pull_request_template.md` で定義された各セクションを削除せずに利用する。  
- 該当しない項目は空欄にせず **`N/A` と明記**する。  
- 「関連 Issue」「変更内容」「Terraform 検証結果」「リスク」「チェックリスト」など、  
  テンプレートに定義された見出しを必ず維持すること。

## 6-2. Terraform の検証結果（必須）

PR には、少なくとも次の検証結果を含める：

- `terraform -chdir=infra fmt -check`
- `terraform -chdir=infra validate`
- `terraform -chdir=infra plan -input=false`

エージェントは、これらのコマンドの **結果概要** を  
PR テンプレートの「Terraform 検証結果」セクションに貼り付ける。  
実行できない場合は、その理由と代替の検証内容を明記する。

## 6-3. GitHub Actions / テスト結果

ワークフローを追加・修正した場合、  
可能な範囲で対象ワークフローを実行し、  
結果を PR テンプレートの「GitHub Actions / その他テスト」セクションに記載する。

## 6-4. スコープ確認

PR の「チェックリスト」セクションで、  
- 対応 Issue のスコープ外の変更を行っていないこと  
- AGENTS.md のルールに従っていること  
をチェックし、必要に応じて補足説明を記載する。

---

# 7. 質問すべきタイミング

推測による実装は禁止。次の場合は必ずユーザーに質問：

- Issue のスコープが曖昧 or 解釈が複数ある  
- 既存コードと衝突する  
- セキュリティ設定が曖昧  
- 変更内容が破壊的になる可能性がある

---

# 8. 実装の品質基準

- シンプルで明快  
- 再現性重視  
- 本仕様との一貫性  
- 構文ミスは絶対に残さない  
- 命名・構造は Issue #1 の仕様に準拠する

---

# 9. この AGENTS.md の目的

Codex エージェントが  
- 安定した  
- 再現性の高い  
- 余計な変更のない  
開発を行うためのルールセットを提供する。

エージェントは **常にこのファイルを最優先** とし、  
疑問点がある場合は Issue ではなく **ユーザーに明確化を求める**。
